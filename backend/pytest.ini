[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*

# Default: run all test files with coverage on the three core modules
addopts =
    --tb=short
    -q
